queue = {}

class Thread:

    def __init__(self, system):
        print("")

    def main():
        while True:
            if queue.__sizeof__ > 0:
                run_thread()

    def run_thread():
        current_task = queue[0]
    
        if current_task.key == "execute_task":


    def queue_task(key: str, execute: str):
        queue[queue.__sizeof__] = {key, execute}